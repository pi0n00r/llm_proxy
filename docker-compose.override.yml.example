version: '3.8'

services:
  llm-proxy:
    ports:
      - 6666:6666
      # Change the host-side port if 6666 conflicts with another service.
    volumes:
      # Mount config file
      - ./config.toml:/app/config/config.toml:ro
      # Mount database directory (allows persistence)
      - ./data:/app/data
